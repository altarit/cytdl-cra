import {connect} from 'react-redux'

import PreviewEntry from '../component/PreviewEntry'

const mapDispatchToProps = {
}

const mapStateToProps = (state) => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(PreviewEntry)
